# In this file , We will set all the files path

from pathlib import Path 
CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")

#we will read those yaml files using the functions defined in our utility 